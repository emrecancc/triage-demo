const assert = require("assert"); // Flaky test: timing-sensitive assertion async function test_async_timing() { const start = Date.now(); await new Promise(resolve => setTimeout(resolve, 50)); const elapsed = Date.now() - start; // This will fail if system is under load (elapsed > 100ms) assert.ok(elapsed < 200, `Expected < 200ms but got ${elapsed}ms`); console.log(`Timing test passed: ${elapsed}ms`); } test_async_timing().catch(err => { console.error("FAIL:", err.message); process.exit(1); });