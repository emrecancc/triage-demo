{
  "name": "triage-demo",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && echo '// placeholder' > dist/index.js",
    "start": "node dist/index.js",
    "test": "echo 'ok'"
  }
}
